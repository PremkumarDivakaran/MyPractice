pipeline {
    agent any
    tools {
        maven 'maven-3.8.2'
        jdk 'jdk8'
    }
    stages {
        stage ('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
                '''
            }
        }
        stage('Build') {
            steps {
                echo 'Build project'
            }
        }
        stage('Test') {
            steps {
                echo 'Run Test after build'
            }
        }
        stage('Report') {
            steps {
                echo 'Report Ready'
            }
        }
    }
}